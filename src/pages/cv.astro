---
import Layout from "../layouts/Layout.astro";
import { profile, skills, experiences, educations } from "../data/cv";
---

<Layout title={`CV - ${profile.name}`}>
  <div class="space-y-6">
    <header
      class="pb-6 border-b border-[#d0d7de] flex flex-col md:flex-row md:items-start justify-between gap-4"
    >
      <div>
        <h1 class="text-2xl font-semibold text-[#1f2328] tracking-tight flex items-center gap-2">
          {profile.name}
          <span
            class="text-xs font-normal px-2 py-0.5 border border-[#d0d7de] text-[#636c76] rounded-full bg-[#f6f8fa]"
          >
            Disponible
          </span>
        </h1>
        <p class="text-base font-medium text-[#0969da] mt-1">{profile.title}</p>
        <p class="text-xs text-[#57606a] font-mono mt-0.5">{profile.subtitle}</p>
        <p class="mt-3 text-[14px] text-[#24292f] leading-relaxed max-w-3xl">
          {profile.details}
        </p>
      </div>

      <div
        class="shrink-0 p-3 bg-[#f6f8fa] border border-[#d0d7de] rounded-md space-y-1.5 text-xs text-[#57606a] font-mono min-w-60"
      >
        <div class="flex items-center gap-2">
          <span class="font-semibold text-[#1f2328] w-14">Web:</span>
          <a
            href={`https://${profile.website}`}
            target="_blank"
            class="text-[#0969da] hover:underline">{profile.website}</a
          >
        </div>
        <div class="flex items-center gap-2">
          <span class="font-semibold text-[#1f2328] w-14">GitHub:</span>
          <a
            href={`https://${profile.github}`}
            target="_blank"
            class="text-[#0969da] hover:underline">GuillaumeBraillon</a
          >
        </div>
        <div class="pt-1.5 border-t border-[#d0d7de] text-[11px]">
          Permis B · {profile.lang}
        </div>
      </div>
    </header>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
      <div class="lg:col-span-2 space-y-6">
        <section>
          <h2 class="text-xs font-semibold text-[#1f2328] uppercase tracking-wider mb-3">
            Expériences Professionnelles
          </h2>
          <div class="space-y-4">
            {
              experiences.map((exp) => (
                <div class="border border-[#d0d7de] rounded-md bg-white p-4 hover:border-[#0969da] transition-colors group">
                  <div class="flex flex-col sm:flex-row sm:items-start justify-between gap-1">
                    <div>
                      <h3 class="text-[14px] font-semibold text-[#1f2328]">{exp.role}</h3>
                      <p class="text-xs font-medium text-[#0969da] mt-0.5">{exp.company}</p>
                    </div>
                    <span class="text-[11px] font-mono text-[#636c76] bg-[#eaeef2] px-2 py-0.5 rounded-full sm:shrink-0 whitespace-nowrap self-start sm:self-auto">
                      {exp.period}
                    </span>
                  </div>
                  <p class="mt-3 text-[13px] text-[#57606a] leading-normal">{exp.description}</p>
                </div>
              ))
            }
          </div>
        </section>

        <section>
          <h2 class="text-xs font-semibold text-[#1f2328] uppercase tracking-wider mb-3">
            Formation & Certifications
          </h2>
          <div class="grid gap-3 sm:grid-cols-2">
            {
              educations.map((edu) => (
                <div class="border border-[#d0d7de] rounded-md bg-white p-3.5 flex flex-col justify-between">
                  <div>
                    <div class="flex items-start justify-between gap-2">
                      <h3 class="text-[13px] font-semibold text-[#1f2328] leading-tight">
                        {edu.title}
                      </h3>
                      <span class="text-[10px] font-mono font-medium text-[#636c76] border border-[#d0d7de] px-1.5 py-0.2 rounded bg-[#f6f8fa]">
                        {edu.year}
                      </span>
                    </div>
                    <p class="text-xs text-[#0969da] font-medium mt-0.5">{edu.institution}</p>
                    <p class="mt-2 text-[12px] text-[#57606a] leading-normal">{edu.details}</p>
                  </div>
                </div>
              ))
            }
          </div>
        </section>
      </div>

      <div class="space-y-4">
        <h2 class="text-xs font-semibold text-[#1f2328] uppercase tracking-wider">
          Compétences Techniques
        </h2>

        <div class="border border-[#d0d7de] rounded-md bg-white overflow-hidden">
          <div class="bg-[#f6f8fa] border-b border-[#d0d7de] px-4 py-2">
            <span class="text-xs font-semibold text-[#1f2328]">IA & Automatisation</span>
          </div>
          <div class="p-3 flex flex-wrap gap-1.5">
            {
              skills.iaAuto.map((skill) => (
                <span class="text-[11px] text-[#1a7f37] bg-[#dafbe1] border border-[rgba(26,127,55,0.2)] px-2 py-0.5 rounded-full font-medium">
                  {skill}
                </span>
              ))
            }
          </div>
        </div>

        <div class="border border-[#d0d7de] rounded-md bg-white overflow-hidden">
          <div class="bg-[#f6f8fa] border-b border-[#d0d7de] px-4 py-2">
            <span class="text-xs font-semibold text-[#1f2328]">Frontend</span>
          </div>
          <div class="p-3 flex flex-wrap gap-1.5">
            {
              skills.frontend.map((skill) => (
                <span class="text-[11px] text-[#0969da] bg-[#ddf4ff] border border-[rgba(9,105,218,0.2)] px-2 py-0.5 rounded-full font-medium">
                  {skill}
                </span>
              ))
            }
          </div>
        </div>

        <div class="border border-[#d0d7de] rounded-md bg-white overflow-hidden">
          <div class="bg-[#f6f8fa] border-b border-[#d0d7de] px-4 py-2">
            <span class="text-xs font-semibold text-[#1f2328]">Backend</span>
          </div>
          <div class="p-3 flex flex-wrap gap-1.5">
            {
              skills.backend.map((skill) => (
                <span class="text-[11px] text-[#0969da] bg-[#ddf4ff] border border-[rgba(9,105,218,0.2)] px-2 py-0.5 rounded-full font-medium">
                  {skill}
                </span>
              ))
            }
          </div>
        </div>

        <div class="border border-[#d0d7de] rounded-md bg-white overflow-hidden">
          <div class="bg-[#f6f8fa] border-b border-[#d0d7de] px-4 py-2">
            <span class="text-xs font-semibold text-[#1f2328]">Bases de données</span>
          </div>
          <div class="p-3 flex flex-wrap gap-1.5">
            {
              skills.database.map((skill) => (
                <span class="text-[11px] text-[#24292f] bg-[#f6f8fa] border border-[#d0d7de] px-2 py-0.5 rounded-full font-medium font-mono">
                  {skill}
                </span>
              ))
            }
          </div>
        </div>

        <div class="border border-[#d0d7de] rounded-md bg-white overflow-hidden">
          <div class="bg-[#f6f8fa] border-b border-[#d0d7de] px-4 py-2">
            <span class="text-xs font-semibold text-[#1f2328]">DevOps & Outils</span>
          </div>
          <div class="p-3 flex flex-wrap gap-1.5">
            {
              skills.devops.map((skill) => (
                <span class="text-[11px] text-[#57606a] bg-[#eaeef2] px-2 py-0.5 rounded-full font-medium">
                  {skill}
                </span>
              ))
            }
          </div>
        </div>

        <div
          class="p-3 bg-[#fff8c5] border border-[#rgba(191,133,0,0.3)] rounded-md text-xs text-[#735c0f]"
        >
          <strong>Hobbies & Intérêts :</strong> Domotique (Home Assistant), Guitare, Photographie & Vidéo,
          Voyages (Australie, Irlande).
        </div>
      </div>
    </div>
  </div>
</Layout>
